{
  "type": "object",
  "required": [
    "type"
  ],
  "additionalProperties": false,
  "properties": {
    "type": {
      "$ref": "../types/ServiceRate.json"
    },
    "attributes": {
      "type": "object",
      "required": [
        "price",
        "purchase_price"
      ],
      "properties": {
        "code": {
          "type": "string",
          "minLength": 1,
          "maxLength": 255
        },
        "weight_min": {
          "type": "number",
          "minimum": 0
        },
        "weight_max": {
          "type": "number",
          "minimum": 0
        },
        "length_max": {
          "type": "number",
          "minimum": 0
        },
        "width_max": {
          "type": "number",
          "minimum": 0
        },
        "height_max": {
          "type": "number",
          "minimum": 0
        },
        "volume_max": {
          "type": "number",
          "minimum": 0
        },
        "price": {
          "$ref": "../includes/price.json"
        },
        "purchase_price": {
          "$ref": "../includes/price.json"
        },
        "fuel_surcharge": {
          "$ref": "../includes/price.json"
        }
      }
    }
  }
}
